import {useState} from "react";import {Link,useNavigate} from "react-router-dom";import api from "../services/api";import {useAuth} from "../context/AuthContext";
export default function Auth({register=false}){const [form,setForm]=useState({gymName:"",ownerName:"",email:"",phone:"",password:""});const [error,setError]=useState("");const {setAuth}=useAuth();const nav=useNavigate();const submit=async e=>{e.preventDefault();setError("");try{const r=await api.post(register?"/auth/register":"/auth/login",form);setAuth({loading:false,...r.data});nav("/")}catch(e){setError(e.response?.data?.message||"Something went wrong")}};return <div className="auth"><div className="authbox"><div className="logo big">A</div><h1>{register?"Create your gym":"Welcome back"}</h1><p>{register?"Start your AURA Gym workspace.":"Sign in to your AURA Gym workspace."}</p>{error&&<div className="error">{error}</div>}<form onSubmit={submit}>{register&&<><label>Gym Name<input required value={form.gymName} onChange={e=>setForm({...form,gymName:e.target.value})}/></label><label>Owner Name<input required value={form.ownerName} onChange={e=>setForm({...form,ownerName:e.target.value})}/></label><label>Phone<input value={form.phone} onChange={e=>setForm({...form,phone:e.target.value})}/></label></>}<label>Email<input required type="email" value={form.email} onChange={e=>setForm({...form,email:e.target.value})}/></label><label>Password<input required type="password" minLength="6" value={form.password} onChange={e=>setForm({...form,password:e.target.value})}/></label><button className="primary wide">{register?"Create Account":"Login"}</button></form><p className="switch">{register?"Already have an account?":"New gym?"} <Link to={register?"/login":"/register"}>{register?"Login":"Register"}</Link></p></div></div>}
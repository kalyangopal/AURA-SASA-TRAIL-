import {useEffect,useState} from "react";import api from "../services/api";import {Link} from "react-router-dom";
const money=n=>"₹"+Number(n||0).toLocaleString("en-IN",{maximumFractionDigits:2});
export default function Dashboard(){const [d,setD]=useState(null);useEffect(()=>{api.get("/reports/dashboard").then(r=>setD(r.data))},[]);if(!d)return <div>Loading…</div>;return <section><div className="head"><div><h1>Dashboard</h1><p>Manage your gym from one simple workspace.</p></div><Link className="primary" to="/members">+ Add Member</Link></div><div className="stats">{[["Total Members",d.total,"All registered members"],["Active Members",d.active,"Currently active"],["Today's Attendance",d.todayAttendance,"Present today"],["Pending Payments",money(d.pending),"Outstanding balance"],["Total Collection",money(d.collected),"Recorded payments"]].map(x=><div className="stat" key={x[0]}><span>{x[0]}</span><strong>{x[1]}</strong><small>{x[2]}</small></div>)}</div><div className="columns"><article className="card"><h2>Quick Actions</h2><div className="quick"><Link to="/members">Manage Members</Link><Link to="/attendance">Mark Attendance</Link><Link to="/payments">Record Payment</Link><Link to="/reports">View Reports</Link></div></article><article className="card"><h2>Recent Payments</h2>{d.payments.slice(0,6).map(p=><div className="row" key={p._id}><span>{p.memberId?.name||"Member"} · {p.date}</span><b>{money(p.amount)}</b></div>)}</article></div></section>}